{
  "scripts": {
    "bundle:openapi": "node scripts/bundle-openapi.mjs",
    "docs:dev": "npm run bundle:openapi && vitepress dev docs",
    "docs:build": "npm run bundle:openapi && vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "export-pdf": "press-export-pdf export ./docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4",
    "vitepress-export-pdf": "^1.0.0"
  },
  "dependencies": {
    "vitepress-openapi": "^0.1.14"
  }
}
